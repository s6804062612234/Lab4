#include <stdio.h>


int main (void) {
	char x;
	int row, column;
	scanf("%c %d %d", &x, &row, &column);
	
	for (int i = 1; i <= column; i++) {
		for (int j = 1; j <= row; j++) {
			printf("%c", x);
		}
		printf("\n");
	}
	
	return 0;
}
