#include <stdio.h>


int main (void) {
	char x;
	int times;
	scanf("%c %d", &x, &times);
	
	for (int i = 1; i <= times; i++) {
		for (int j = 1; j <= times; j++) {
			printf("%c", x);
		}
		printf("\n");
	}
	
	return 0;
}
